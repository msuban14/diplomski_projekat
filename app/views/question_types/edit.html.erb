<%= page_heading "edit question type #{@question_type.name}" %>
<h1>Editing Question Type <%= @question_type.name%></h1>

<%= render 'form', question_type: @question_type %>

<%= link_to 'Show', @question_type %> |
<%= link_to 'Back', :back %>
